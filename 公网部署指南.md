
# 🚀 心跳互联 - 公网部署指南

## 🎯 目标
部署后，您和任何人可以在**全球任何地方**使用心跳互联，**不需要同一网络**，**手机流量也可以**！

---

## 📋 第一步：下载并安装 ngrok

### 1.1 下载 ngrok

访问：**https://ngrok.com/download**

点击 **Download for Windows** 下载 ZIP 文件

### 1.2 解压安装

1. 下载完成后，右键点击 ZIP 文件
2. 选择 **"全部解压缩..."**
3. 解压到一个容易找到的位置，例如：
   - `C:\tools\ngrok`
   - 或 `桌面\ngrok`

### 1.3 配置环境变量（可选）

为了让 ngrok 在任何地方都能运行，您可以：

1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击 **高级** 选项卡 → **环境变量**
3. 在 **系统变量** 中找到 **Path**，双击编辑
4. 点击 **新建**，添加 ngrok.exe 所在文件夹的路径
5. 确定保存

---

## 📋 第二步：注册 ngrok 账号（免费）

### 2.1 访问官网注册

访问：**https://dashboard.ngrok.com/signup**

### 2.2 填写注册信息

- 邮箱地址
- 密码
- 确认密码

### 2.3 验证邮箱

- 登录您的邮箱
- 找到 ngrok 发送的验证邮件
- 点击验证链接

---

## 📋 第三步：获取 Authtoken

### 3.1 登录 ngrok Dashboard

访问：**https://dashboard.ngrok.com**

### 3.2 找到您的 Token

1. 登录后，在左侧菜单点击 **Getting Started**
2. 点击 **2. Connect your account**
3. 您会看到类似这样的命令：

```bash
ngrok config add-authtoken 2h1xXXXXXXXXXXXXXXXXXXqL
```

**复制这个命令！**

---

## 📋 第四步：配置 ngrok

### 4.1 打开终端

按 `Win + R`，输入 `cmd`，回车

### 4.2 配置 Authtoken

在终端中**右键粘贴**以下命令（将 `您的token` 替换为第三步复制的token）：

```bash
ngrok config add-authtoken 您的token
```

例如：
```bash
ngrok config add-authtoken 2h1xXXXXXXXXXXXXXXXXXXqL
```

### 4.3 验证配置

输入：
```bash
ngrok version
```

应该显示版本号，说明安装成功！

---

## 📋 第五步：启动公网隧道

### 5.1 确保心跳互联服务器正在运行

**如果没有运行**，请双击：
```
d:\trae solo\01\heartbeat-connect\启动心跳互联.bat
```

等待看到：
```
🚀 Heartbeat Connect Server running on port 3000
```

### 5.2 启动 ngrok 隧道

**打开一个新的终端窗口**（不要关闭服务器窗口！）

输入：
```bash
ngrok http 3000
```

### 5.3 获取公网地址

成功后会看到类似这样的界面：

```
Session Status                online
Account                       your@email.com (Plan: Free)
Forwarding                    https://abc123def456.ngrok.io -> http://localhost:3000
```

**重要！复制 `https://abc123def456.ngrok.io` 这个地址！**

---

## 📋 第六步：使用心跳互联

### 6.1 访问应用

在**任何设备**的浏览器中打开**刚才复制的地址**：

```
https://abc123def456.ngrok.io
```

**注意：** 后面不需要加端口号，ngrok 已经自动映射了！

### 6.2 配对使用

1. 用户A打开链接 → 输入昵称 → 生成配对码
2. 用户B打开**同一个链接** → 输入昵称 → 输入配对码 → 加入
3. 开始互动！

---

## 🎉 恭喜！部署成功！

### ✅ 现在您拥有：
- ✅ **公网访问地址**：`https://abc123def456.ngrok.io`
- ✅ **全球可访问** - 任何地方都能用
- ✅ **无需同一网络** - 手机流量也可以
- ✅ **免费使用** - ngrok 免费版完全够用

### 📱 使用方式：
- 在微信/QQ 中分享链接
- 发给朋友
- 任何人都可以打开使用

---

## ⚠️ 重要提示

### 关于 ngrok 免费版：
- ✅ **永久免费**
- ✅ **带宽限制**：40连接/分钟
- ✅ **同时在线**：最多1个隧道
- ⚠️ **重启后会变化**：每次重启 ngrok，地址会变

### 保持服务运行：
1. **不要关闭**运行 `ngrok http 3000` 的终端窗口
2. **不要关闭**运行心跳互联服务器的窗口
3. 如果电脑休眠，服务会断开

### 关机/重启后：
需要重新启动 ngrok，会得到**新的地址**

---

## 🔄 如何更新应用

如果修改了代码：

1. 修改 `d:\trae solo\01\heartbeat-connect\app\index.html`
2. 重启 ngrok（按 `Ctrl + C` 停止，然后重新 `ngrok http 3000`）
3. 用新的公网地址访问

---

## 🎯 推荐：固定地址方案

如果想要**固定不变的地址**，可以：

### 方案一：ngrok 付费版
- 每月 $5 起
- 可以保留固定子域名

### 方案二：部署到云服务器
- Railway (免费): https://railway.app
- Render (免费): https://render.com
- 24/7 运行，固定地址

---

## 💡 使用技巧

### 快速分享
1. 把公网地址发到微信
2. 朋友点击打开
3. 两个人都能看到同一个页面

### 收藏链接
- 建议把地址收藏在微信笔记
- 或截图保存
- 方便下次使用

### 测试
1. 用两部手机分别打开链接
2. 测试配对功能
3. 确认震动正常

---

## 🆘 常见问题

### Q: ngrok 显示 "online" 但打不开？
A: 检查：
   1. 心跳互联服务器是否还在运行
   2. 地址是否输入正确（https 前缀）
   3. 网络是否正常

### Q: 地址变了怎么办？
A: 免费版每次重启都会变，只能：
   1. 重新获取地址
   2. 重新分享给朋友

### Q: 能用多久？
A: 
   - 只要电脑开着，ngrok 就能用
   - 免费版建议连续使用不超过几小时
   - 建议需要时再启动

---

## 📞 下一步

1. ✅ 下载 ngrok
2. ✅ 注册账号
3. ✅ 配置 authtoken
4. ✅ 启动隧道
5. ✅ 分享地址开始使用！

---

**现在就去 https://ngrok.com 下载吧！** 🚀

需要帮助随时问我！
